# Hebrew-N-gram-Word-Prediction-System
project implements a MapReduce system for Hebrew word prediction using Google's n-gram dataset. The system calculates conditional probabilities for trigrams (three consecutive words) to predict the most likely next word given a pair of preceding words.
